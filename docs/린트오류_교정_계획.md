# 린트 오류 교정 계획서 (Fix Unused Import in LogPages.tsx)

이 계획서는 프론트엔드 빌드 시 ESLint 정적 분석에서 감지된 미사용 임포트 오류를 해결하는 방안을 기술합니다.

---

## 1. 오류 원인
- `LogPages.tsx`의 2번 라인에서 `lucide-react`로부터 `ArrowDown` 아이콘을 임포트했으나, 실제 소스 코드 내부에서는 사용되지 않고 있습니다.
- 프로젝트의 엄격한 ESLint 설정(`@typescript-eslint/no-unused-vars`)에 의해 미사용 변수로 간주되어 빌드 에러가 발생합니다.

---

## 2. 해결 방안
- [LogPages.tsx](file:///home/kdy987/work/aman/frontend/src/domains/system/LogPages.tsx) 파일의 임포트 선언문에서 `ArrowDown`을 삭제합니다.

---

## 3. 검증 계획
- `./fm.sh build` 명령을 가동하여 프론트엔드 빌드가 문제없이 성공하는지 검증합니다.
