# 프롬프트

## 개요

- 개발하면서 사용한 프롬프트

## 내용

1. 지금 component 이름과 구조가 좋지 않음.
2. '/'->'/docs' 이고 여기에서 '일반사용자' 들을 위한 전체 매뉴얼을 보여주게 됨.
3. 그런데 이 component의 이름이 FolderTree인 것임. 
4. NormalUserMain.tsx가 맞을 듯.
위 1-4는 나의 의견이고 아래를 수행해줘.
5. NormalUserMain.tsx를 domain/docs 에 생성할 것.
6. NormalUserMain.tsx는 아무 내용없이 그냥 '일반사용자 AssetERP 매뉴얼 제공' 이라고 문자만 써 줄 것.
7. '/'->'/docs' -> NormalUserMain이 나오게 할 것 

``
        <Route path="/" element={<Navigate to="/docs" replace />} />
        <Route path="/docs" element={<FolderTree />}>
          <Route index element={<NormalUserMain />} />
          <Route path="page/:page_id" element={<MarkdownViewer />} />
        </Route>
``
위 구조에서 FolderTree -> NormalUserMain 으로 이름이 변경되어야한다고 생각함.


ManualMain.tsx의 이름을 NormalUserIntro.tsx로 변경하고 docs폴더로 이동,
NormalUserIndex.tsx는 삭제하고 그 대신 NormalUserIntro.tsx을 사용해줘



http://localhost:5173/admin 으로 접속했었을 때

1. 로그인 상태가 아니라면 (판별가능한가?) 로그인 화면
2. LoginPage.tsx 화면 생성 
3. 로그인 상태라면 content/DocUserMain.tsx생성
4. DocUserMain.tsx은 examples/content4.html을 참조하여 만든다.
  1. 상단에 DocUserTopBar.tsx **새로 생성**
  2. 왼쪽에 FolderTree, 오른쪽 부분은 content4.html을 참조.
  
일단 여기까지 해보자


DocUserMain 을 해 보자. markdown으로 도움말페이지를 만들어서 db에 저장하는 기능을 해야함.

1. 왼쪽 Tree가 선택되었을 때 
    1. db에서 메뉴(folder)에 해당하는  pages를 찾아서 보여준다. 만약 없는 경우에는 현재 비어 있다는 메세지를 보여주고 역시 편집,저장이 가능해야한다.
    2. 메뉴레벨 1,2 역시 page를 작성할 수 있어야한다.
    
일단 여기까지 

수정 요구사항
1. '문서 제목을 입력하세요' 이 부분 필요없은 삭제 요망.
2. 시스템설정 > 문서작성 > 새 도움말페이지 -> 메뉴 체계의 빵부스러기여야 함 
   1000 문서작성 > 1100 문서 작성 이렇게 보이면 됨.
3. 변경사항 저장하기 동작하게끔 수정


## pages 테이블에 aka 필드를 추가할 필요가 있어.

즉 내가 '1110 신규 문서작성' 을 추가하고 aka 에 값을 1110 이라고 넣었다면 
그 페이지의 url은 'baseurl/manual/1110' 으로 접속시 그페이지의 html을 보여주는 것이지.
asseterp의 실제 프록그렘에서 ? 클릭시 html이 팝업으로 뜨는 형식으로 사용하려고 하는 것임.
이해함?
1. 내가 ddl에 aka 추가함. 현재 테이블을 백업받고, 새로운 테이블을 만들고 folders를 백업받은데서 가져와서 채우는 작업을 해줘.
2. /manual/<aka> 백엔드 코딩해줘. (free pass여야함.) ManualController 작성필요할 듯.
3. DocUserMain.tsx에 aka를 입력받는 EditorToolbar에 넣으면 어떨까? 
4. 저장된 후 url을 upsert버튼옆에 표시하고 복사버튼도 있으면 좋겠어.


## 수정사항

1. DocUserTopBar에서  'A-Man version'  ' A-Man (1.0.0)'과 같이 버젼을 표시 /health에 version이 들어 있음.
2. DocUserMain에서 
``
          {/* Breadcrumbs */}
          <nav className="flex items-center space-x-2 text-[11px] text-gray-400 font-medium mb-2.5 shrink-0">
            {folderHierarchy.length > 0 ? (
              folderHierarchy.map((item, idx) => (
                <React.Fragment key={item.id}>
                  {idx > 0 && <ChevronRight className="w-3 h-3 text-gray-300" />}
                  <span className={idx === folderHierarchy.length - 1 ? "text-indigo-600 font-semibold" : "text-gray-400 font-medium"}>
                    {item.nums ? `${item.nums} ` : ''}{item.name}
                  </span>
                </React.Fragment>
              ))
            ) : (
              <>
                <span className="hover:text-gray-600 cursor-pointer">시스템 설정</span>
                <ChevronRight className="w-3 h-3 text-gray-300" />
                <span className="text-gray-400 font-medium">도움말</span>
              </>
            )}
          </nav>
        `` 이것을 shared에 FolderBreadcrumbs.tsx로 빼면 좋을 듯.
3.    DocUserMain에서 315~ 482를 .ts로 빼는 것은 어떨지?

``
              <h1 className="text-xl font-bold text-gray-900">
                {page.folder ? `${page.folder.nums ? page.folder.nums + ' ' : ''}${page.folder.name}` : '새 도움말 페이지'}
              </h1>
``
위 소스가 '1110 문서작성' 과 같이 표시함 이것을 '문서작성(1111)'과 같이 표현하고 싶음 소스를 어떻게 수정해야할까?

## Assets기능

1. '이모지','상용구','템플릿' 이 3가지 기능을 위한 테이블을 생성
2. assets
```sql
CREATE TABLE assets (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    atype       TEXT NOT NULL,                      -- 자산 타입 (EMOJI, PHRASE, TEMPLATE, SYMBOL)
    name        TEXT NOT NULL,                      -- 자산 이름 / 이름표
    value       TEXT NOT NULL,                      -- 실제 데이터 본문 (마크다운, 기호, 텍스트 등)
    created_at  DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at  DATETIME DEFAULT CURRENT_TIMESTAMP,

    -- 약속된 키워드 외의 텍스트가 들어오는 것을 방지
    CONSTRAINT chk_atype CHECK (atype IN ('EMOJI', 'PHRASE', 'TEMPLATE', 'SYMBOL'))
);

-- 자주 사용될 타입별 검색을 위한 복합 인덱스
CREATE INDEX idx_assets_atype_name ON assets(atype, name);
```
3. assets 테이블을 문서관리자들이 CRUD하도록 함.
4. markdown 편집기에서 4개의 button 제공, 현재 이모지 버튼은 존재함.


## status 도입

1. status상태를 파악하여 문서의 제공 여부 판단
 - /docs 에서 메뉴 클릭시 page의 status가 PUBLISHED일 경우만 markdown을 html로 제공해서 보여준다.
 - /manual/{id} 에서도 마찬가지로 PUBLISHED에서만 제공한다.
2. '문서편집'에서 즉 DocUserMain에서 
  - '변경사항 저장하기' 버튼 앞에 DRAFT/PUBLISHED -> 작성 중 / 완료 및 배포 switch를 둔다.


## 수정 사항

### v 1.1.0

1. Folder 필터링 에서 현재는 name으로만 찾는데, nums에서도 찾게 끔 해줘.
2. DocUserMain 문서편집에서 미리보기와 scroll sync
3. 개별문서 URL에 explore icon추가
4. '삭제하기' 버튼 생성 -> 페이지를 삭제 가능
5. '가져오기' 중첩zip 가능
6. 링크를 target=_blank로 열기
7. aka별칭을 '문서 번호'로  우선적으로 설정

### v 1.1.2

1. Markdown Editor 부분 수정
  1. H-> H1~H6 판넬로 클릭하면 '#'을 커서에 넣게끔.
  
  1. '<>' 즉 소스 icon은 삭제
  2. 'B','I','S' 묶음으로 Bold, Italic, Strike , Ctrl+B, Ctrl+I, Ctrl+T

  4. context메뉴가 커서의 위치에 따라서 커서의 상단에 뜨도록 해야할 듯. 
  5. markdown 편집영역과 preview 영역을 splitter로 나누어서 width를 조절하는데. 편집영역이 늘어나지는 않음. 다소간 늘어나게끔.

### v1.4.5

#### RangeSlider의 기능 개선

- 나는 이것을 '버튼증감(ButtonIncDev)'기능이라고 부르겠다. 
- slider 양 옆에 삼각형 아이콘을 두자.
- 왼쪽은 내림 삼각형 즉 꼭지점이 아래, 오른쪽은 꼭지점이 위로 된 삼각형
- Props
  1. button_inc_dev_enable : true/false, false이 버튼 안나옴. default false
  2. button_inc_dev_step : 클릭시 +,- step 양


```tsx
        <button
          type="button"
          onClick={handleConfirm}
          className="text-xs px-2 py-0.5 bg-gray-300 hover:bg-indigo-500 text-white rounded cursor-pointer font-bold h-6 flex items-center justify-center transition-colors"
        >
          확인
        </button>
      )}
      <button
        type="button"
        onClick={handleCancel}
        className="text-xs px-2 py-0.5 bg-gray-300  hover:bg-gray-500  text-white  rounded cursor-pointer font-bold h-6 flex items-center justify-center transition-colors"
      >
        취소
      </button>
```     
#### 바탕확장(CanvasExpand)기능

1. 예를 들어 TopBar 메뉴를 잘라내어서 각 메뉴버튼에 대한 설명을 한다고 가정하자.
2. 가로길이가 긴 세로높이는 작은 길쭉한 이미지이다.
3. 이 이미지에 라인과 텍스트를 넣어서 설명하려면 이미지 위에서 좀 곤란하다.
4. 그래서 바탕 canvas를 늘릴 필요가 있지 않겠는가!
5. 예를 들아 아래로 100px 늘리면 지금이미지들 아래에 하얀색 바탕이 나타나고 거기에 item들을 또 배치할 수 있지 않을까?



ActionImageEditor.tsx에서 어제 bracket-arrow 을 만들었는데. 자세히 보니 orthogonal-arrow을 선택한 후에 중앙점을 상하로 움직이면 bracket-arrow를 만들 수 있지 않을까?
하는 생각이 듬. 만약 그게 된다면 bracket-arrow관련된 코드를 삭제할 수 있을 듯.
코드를 보고 확인해 주면 좋겠음.


orthogonal-arrow을 선택했었을 때 화살표 양끝점과 중앙점이 있음. 
중앙점은 horizontal 즉 x축으로만 움직임. 그러데 y축으로 움직임을 만들어서 라인을 그린다면  冖 나 반대되는 모양의 라인이 생성될 수 있다고 생각함.
다시 한번 고려보기 바람. 어제 orthogonal-arrow 를 만들때도 그러하였음.
만약 그것이 된다면 bracket-arraw icon을 제거하고 관련 코드를 삭제할 수 있음.
